<%= simple_form_for([article, review], remote: true) do |f| %>
  <%= f.input :text, :label => "Comment something about it" %>
  <%= f.submit value: 'Comment', class: 'btn btn-warning btn-xs' %>
<% end %>
